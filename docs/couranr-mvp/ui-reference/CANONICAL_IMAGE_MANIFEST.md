# Canonical image manifest

The approved canonical MVP image set contains 62 PNG files grouped into public, merchant, driver, operations, and customer surfaces. Paths are defined by `UI_SCREEN_REGISTRY.md`.
