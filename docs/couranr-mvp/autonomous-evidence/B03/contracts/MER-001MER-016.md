BUILD CONTRACT — Couranr merchant-workspace screens (MER-001…MER-016)
Extracted 2026-08-06 from repo authorities. Every claim cites file:line from commands run in /home/user/couranr-os.

=== GLOBAL FACTS THAT BIND EVERY SCREEN ===

- CLAUDE.md is STALE on two points: `02_DECISION_REGISTRY.json` DOES exist at repo root (2,574 lines, 43 decisions) and `supabase/migrations/` DOES exist (42 applied migration files, 20260731045417 → 20260806120353). Verified by `ls`.
- Shell/guard: all merchant pages render inside `MerchantShell` + `SurfaceGuard surface="business"` (`app/(couranr)/business/layout.tsx:17-22`). Guard is UX-only; authorization lives in named server commands. Nav for settings screens: `lib/couranr/navigation.ts:142-144`.
- Actor resolution: `lib/couranr/requests/actor.ts` — Bearer token → `resolveRequestActor`/`resolveUserId`; profile role `admin` = Operations (`actor.ts:102-110`); memberships read `business_account_id,user_id,role,status` (`actor.ts:125`).
- Roles (DRP-001, decided, registry): create/submit = owner|manager|dispatcher; viewer|billing denied; invited/disabled denied; Operations read/review only. Implemented as pure matrix `canActOnDeliveryRequest` (`lib/couranr/requests/permissions.ts:40,54-80`).
- Roles (TRM-002, decided 2026-08-06, registry): conversations ONLY — owner/manager/dispatcher read+send; viewer/billing NO access (read refused too). Already implemented: read gate at `lib/couranr/conversations/commands.ts:145-160`, list filter at `:308-327`, send gate at `:548-587`. Code must cite TRM-002, never DRP-001. TRM-002 explicitly does NOT define any other permission — every non-conversation, non-request role distinction (e.g. billing-only screens) is an OPEN decision; safe behavior = membership-scoped read, write only where DRP-001/TRM-002 grant.
- State groups are independent (STA-001, decided): request(9) / payment(10) / readiness(5) / review(5) / incident(6); fulfillment has 14 values (STA-002). Never collapse into one status (UI_SCREEN_REGISTRY.md:307). Live in `couranr_delivery_requests` checks (migration 20260731045417:121-136) and `couranr_deliveries.fulfillment_state` (migration 20260801083000:186,211).
- Every transition is a named server command with verified actor (TRN-001; spec 01_MASTER_IMPLEMENTATION_SPEC.md:318). Money: integer cents, server-computed; client amount keys are actively refused (`lib/couranr/onboarding/workspace.ts:118-146`; `couranr_dr_*` checks forbid rounding/tax/payment_due: migration 20260731045417:158-166).
- Pricing implemented per registry: BASE_PRICE_CENTS=2299, tiers from mile 4 at 225¢ (`lib/couranr/pricing/policy.ts:21,38-39`) — PRC-001/MIL-001/MIL-002 satisfied in the couranr slice (legacy `lib/delivery/policy.ts` remains the defect, do not touch).
- Unresolved decisions gating merchant UI: PRC-004 ($0.25 rounding trigger — DB constraint `couranr_dr_no_rounding_chk` enforces rounding_applied=false, keep it); TAX-001 (tax — `couranr_dr_no_tax_chk`; MER-016 must show no tax line); SVC-002 (market boundaries); OVN-002 (overnight request mechanism); FLG-002 (flag storage). Safe behavior: render nothing that presumes the decision; surface "captured for Couranr review" paths (SVC-001).
- Mocks: `docs/couranr-mvp/ui-reference/CANONICAL_SCREEN_SOURCE_MAP.tsv` maps every MER screen to root-UUID PNGs (all exist on disk at repo root; verified). Only 2 named canonical images exist under `canonical-mvp-images/merchant/` (MER-008, MER-010). The registry's `canonical-mvp-images/merchant/MER-0xx_*.png` paths for the other 14 do NOT exist — use the root PNG via the TSV; never delete a root PNG. Mocks lose to written spec (`lib/couranr/screens.ts:8-11`).
- Verification obligation (all screens): drive in a real browser via Playwright at `/opt/pw-browsers` against `npm run dev`; disposable stack `e2e/disposable/up.mjs` (local Postgres, fidelity check `up.mjs:185-208`) + `gateway.mjs` (PostgREST + real-refusing `/auth/v1`; refusals proved first by `authGateway.mjs:1-24`) signs in real users; assert DB rows via `psql` export `DEPLOY_PROOF_DB_URL` (`up.mjs:257`). Screenshot per required state, each backing a named assertion. Never mutate real rows in the connected project (42 orders/29 deliveries/94 addresses live).

=== PER-SCREEN CONTRACTS ===

MER-001 Merchant dashboard — `/business` — app/(couranr)/business/page.tsx (currently ScreenPlaceholder; `implemented:false` in lib/couranr/screens.ts:60)
- Registry (UI_SCREEN_REGISTRY.md:266-275): today's deliveries, readiness, attention items, payment states, messages, quick actions. Constraint: NO fabricated revenue/customer/on-time metrics — real posted data only; empty state with next action beats a number.
- Data sources that EXIST: GET /api/couranr/delivery-requests?businessAccountId= (list, `listDeliveryRequests` lib/couranr/requests/commands.ts:914-939, membership-checked); GET /api/couranr/conversations (unread counts, TRM-002-filtered commands.ts:308-327); GET /api/couranr/me/business-accounts (tenant picker); GET /api/couranr/delivery-requests/[id]/fulfillment (per-request delivery + payment state, route.ts:24,65-78). Net-new: an aggregate "today" endpoint (or client-side derive from list) — do NOT invent metrics tables.
- Required states: new workspace (no requests → CTA to /business/deliveries/new); empty; active day; degraded payments (obligation failed); activation incomplete (blocked until MER-003 exists — show checklist entry card once activation table lands).
- Roles: any active member reads (DRP-001 read); quick-action "Create delivery" hidden/disabled for viewer/billing.
- Verify: seed 2 tenants on disposable stack, prove tenant A rows never render for tenant B member; screenshot each state.

MER-002 Merchant onboarding — `/business/onboarding` — IMPLEMENTED (screens.ts:61)
- Page app/(couranr)/business/onboarding/page.tsx renders `OnboardingForm`. Command chain: POST /api/couranr/me/workspace (route requires Idempotency-Key; owner = authenticated caller only, me/workspace/route.ts:18-23) → `createMerchantWorkspace` (lib/couranr/onboarding/commands.ts:70-151, refuses admin-profile owners twice: TS :93-110 and SQL CR403 :126-127) → single RPC `couranr_create_merchant_workspace` creating business_account + active owner membership + workspace row atomically (commands.ts:16-22).
- Table `couranr_merchant_workspaces` (migration 20260731061356:62-110): business_category (11-value check verbatim from spec §5 category list, mirrored in `BUSINESS_CATEGORIES` workspace.ts:22-34, drift-tested by tests/couranr-workspace.test.ts per workspace.ts:18-20), pickup_address jsonb, contact_phone, payer_default merchant|customer, policies_accepted_at/version (POLICIES_VERSION="couranr-policies-2026-07" workspace.ts:45), unique per business_account and per (created_by, idempotency_key). service_role-only grants (:120-123).
- GAPS vs registry (UI_SCREEN_REGISTRY.md:282-283): secondary categories (spec allows up to 3; deliberately not collected — migration comment :68-69) — additive column later; payer_default lacks "it depends on the order" option (PAY-001 says 3 options, default "it depends"; DB check :107 allows only merchant|customer) — closing this needs an ADDITIVE migration widening the check + workspace.ts:206-209. Required states "verification pending / suspicious-business review / saved draft" are net-new.
- Constraint: keep light — no Stripe/logo/website tools/activation here (registry :285; workspace.ts:8-11 agrees).

MER-003 Live activation checklist — `/business/onboarding?step=activation` — MISSING ENTIRELY (screens.ts:62 implemented:false; grep for activation in lib/ and migrations returns only comments)
- Activation state machine, extracted from authorities:
  - Master Package :215-217 / spec 01:158-160: test workspace (= what MER-002 creates) "cannot dispatch live deliveries". Live activation REQUIRES: (1) verified operations contact, (2) accepted delivery terms, (3) prohibited-item policy acceptance, (4) merchant responsibility acknowledgement, (5) return acceptance, (6) required operating information. Spec 01:162: do NOT require website, EIN, storefront, or business-registration upload unless risk review requires it.
  - Registry allowed actions (UI_SCREEN_REGISTRY.md:293): accept delivery terms; prohibited-item policy; responsibility+return acknowledgement; verify operations contact; run test delivery; request activation.
  - Registry required states (:294): not_started → in_progress → pending_couranr_review → live | blocked (with reason). That IS the activation state machine; Couranr review is the gate between "requested" and "live" — activation is Operations-granted, never self-granted (TRN-001 actor authority: operations controls review).
  - Master Package :900-902 (Phase 4): includes "simulated test delivery"; acceptance: "no card required to finish onboarding; test delivery creates no live charge". Release matrix :1246: "Test versus live activation" is a required test group.
  - Constraint (registry :296): do NOT require website tools or subscription purchase to activate.
- What exists to build on: `couranr_merchant_workspaces` (one row/account; policies_accepted_at covers the MER-002 basic policy only — the four activation acknowledgements are separate, versioned acts) and the command/event pattern (`couranr_delivery_request_events` shape, migration 20260731045417:228-263; SQLSTATE CR403/CR404/CR409/CR422 convention).
- NET-NEW schema (additive migration, reviewed before apply, service_role-only grants + RLS-enabled matching migration 20260731061356:118-123 pattern; never widen pg_default_acl assumptions — connected project's default ACL grants everything to all roles, so explicit REVOKE ALL first, as the workspace migration does):
  - `couranr_workspace_activations` (1:1 workspace): activation_state check in ('not_started','in_progress','pending_couranr_review','live','blocked'), blocked_reason (server-derived merchant-safe copy per the REV-002 pattern: code→message in DB, internal note never returned), version int for compare-and-set, requested_at/reviewed_at/reviewed_by.
  - `couranr_activation_acknowledgements` (append-only): workspace_id, ack_kind check in ('delivery_terms','prohibited_items','merchant_responsibility','return_acceptance'), version text (per-document version string like POLICIES_VERSION), accepted_by, accepted_at, idempotency_key unique per (workspace, kind, version).
  - contact verification + test-delivery evidence: columns on the activation row (contact_verified_at; test_delivery_request_id FK → couranr_delivery_requests) — the "test delivery" is a real couranr_delivery_requests row flagged so it can never reach payment capture (no live charge; the payment slice's obligation creation must refuse it).
  - Named SQL commands: `couranr_accept_activation_ack`, `couranr_verify_activation_contact`, `couranr_record_test_delivery`, `couranr_request_activation` (guards: all four acks at current versions + contact verified + test delivery recorded, else CR409), `couranr_grant_activation` / `couranr_block_activation` (operations actor only, CR403 otherwise) — each writes an event row in one transaction, TRN-001-style.
- Roles: acks/request-activation = owner (safest reading; acknowledgements are account-level legal acts — DRP-001/TRM-002 don't cover this, so it is an open decision; safe default owner-only, note it). Operations grants/blocks. Merchant can read state, never write review outcome (mirror REV-001 acceptance: "merchants can read the outcome … and cannot write one").
- Enforcement point beyond the UI: submit/dispatch paths must check activation_state='live' before a request can produce a real delivery — enumerate ALL of them when wiring (submit route, operations accept-as-quoted, payment authorize) per CLAUDE.md protocol step 2.
- Verify: disposable stack — walk not_started→in_progress (each ack row asserted by psql)→pending_couranr_review→ operations grant → live; blocked path with reason copy; assert a non-live workspace's request cannot be confirmed; screenshot all five states.

MER-004 Deliveries list — `/business/deliveries` — PLACEHOLDER
- Data: GET /api/couranr/delivery-requests (exists; commands.ts:914-939, limit 1-200, newest first, tenant-scoped by membership). View fields already carry all four state groups + quote (lib/couranr/requests/view.ts:20-63). Fulfillment state per row needs either N calls to /fulfillment (exists, route.ts:24) or a net-new batched list endpoint — prefer extending the list query to join couranr_deliveries (additive).
- Registry (:299-308): filter/search/create/open/duplicate/cancel-when-allowed/mark-ready. Constraint: never collapse state groups — render request/payment/readiness/fulfillment as separate badges. "Duplicate" and "cancel" are NET-NEW commands (no cancel command exists in lib/couranr/requests/commands.ts; CAN-001 fee model applies once confirmed — before authorization cancel is free). Mark-ready exists: POST /[id]/readiness (route maps destination → 4 named SQL commands, readiness/route.ts:13-19; migration 20260801090000:181-224).
- States: all canonical combinations + empty + error. Roles: read = any active member; create/mark-ready = owner/manager/dispatcher (DRP-001).

MER-005/MER-006 Create delivery + review — `/business/deliveries/new(?step=review)` — IMPLEMENTED (screens.ts:64-65)
- One route, two steps via useSearchParams (`NewDeliveryFlow` components/couranr/requests/NewDeliveryFlow.tsx:39-41,84-149). Existing chain: POST /api/couranr/delivery-requests (draft + server-side pricing; Idempotency-Key required, route.ts:24-45) → estimate → submit routes. Pricing: PRC-001/MIL-001/MIL-002 values live in lib/couranr/pricing/policy.ts:21,38+.
- GAP: "Smart Intake" AI parsing is NET-NEW (spec 01:196-226: parse → editable draft → source/confidence/evidence per field → one clarification → deterministic pricing; AI never prices/confirms/captures/selects vehicle; confidence bands 85/60; never invent the :222 field list; review triggers :224). Current flow is the manual-form fallback only — which is itself a required state ("AI unavailable/manual fallback", registry :316), so the existing flow is the fallback branch, kept.
- MER-006 payment: merchant-paid authorize = POST /[id]/authorize-payment (no amount in body by construction, route.ts:15-25); customer link = POST /[id]/payment-link (raw token returned exactly once, only hash stored, route.ts:16-31). Payer per PAY-001: both options every delivery. No silent price change (registry :329); requote paths return stable conflicts (CR409 vocabulary).
- Roles: create/submit owner/manager/dispatcher only (permissions.ts:40).

MER-007 Delivery detail — `/business/deliveries/[id]` — IMPLEMENTED, most complete screen
- Page → `DeliveryRequestDetail` composing MerchantPaymentPanel, MerchantReadinessPanel, MerchantProofPanel, DeliveryExecutionTimeline, fulfillment fetch that FAILS CLOSED (DeliveryRequestDetail.tsx:23-31,168-184,340-375). Data: GET /[id] (route.ts:12-38), GET /[id]/fulfillment (servicePlan + delivery.fulfillmentState, route.ts:65-78), pickup/recipient codes (app/api/couranr/merchant/deliveries/[id]/{pickup-code,recipient-code,proof}/route.ts), conversations.
- Constraint (registry :340): merchant cannot assign, mark picked-up/delivered, refund, or mutate arbitrary state — already structurally true (no such merchant routes exist; verified by route listing). Merchant CAN: mark ready (4 named commands), message, authorize/send link, read review outcome incl. decline copy (REV-002: 8 codes, merchant message derived server-side, internal notes never returned — migration 20260731210000).
- GAPS: "request change", "approve merchant-paid requote", "cancel when allowed" (CAN-001 stage fees), return/incident states — net-new commands. States: all lifecycle + proof processing + return + incident + payment failure.

MER-008/009 Customers list/detail — `/business/customers(?customer=)` — PLACEHOLDER; fully NET-NEW data layer
- Spec tables (01:480): `merchant_customers`, `customer_addresses` — none exist in migrations (grep confirms). Additive migration + named commands (create/edit/archive), tenant-scoped, service_role grants pattern. Registry: no payment credentials or unnecessary PII in list (:351); customer notes merchant-scoped, Couranr does not own the relationship (:362). States: empty/active/archived/duplicate-warning; detail adds no-deliveries/active/conflicting-address. "Repeat delivery" = prefill /business/deliveries/new from a prior request (existing draft POST). Do not fabricate delivery history — join real couranr_delivery_requests rows by recipient.

MER-010/011 Presets list/builder — `/business/presets(?edit=)` — PLACEHOLDER; NET-NEW
- Spec 01:186-194: hierarchy global→customized→created; global updates never overwrite customization; every preset versioned; each delivery stores preset id+version+immutable snapshot; presets may SUGGEST item/count/handling/proof/vehicle-capabilities/questions/payer but never silently assert exact weight/dimensions/value/final vehicle/price/loading/safety (registry :373,384). Tables (spec 01:483): category_presets, category_item_presets, merchant_item_presets, merchant_item_preset_versions — none exist. Applying a preset feeds the existing draft POST; snapshot column on couranr_delivery_requests is additive. States: global-recommendation/customized/created/update-suggested/archived; builder: new/edited/version-conflict (compare-and-set)/recommendation-available.

MER-012 Messages and support — `/business/messages` — SUBSTANTIALLY IMPLEMENTED
- Page renders `ConversationList` (business/messages/page.tsx). API: GET/POST /api/couranr/conversations, [id], [id]/messages, [id]/read — all named commands, service-role with per-query scoping, no role holds INSERT except service_role (conversations/commands.ts:41-46). TRM-002 fully enforced (read :145-160, list :308-327, send :548-587). Response deadline: currently flat 15-minute (states.ts) — HRS-002 requires operating-hours-aware deadline in America/New_York; DB clock functions exist (migration 20260806010000:29-158: couranr_is_within_operating_hours, couranr_add_operating_minutes…) — wiring them into responseDueAt is the remaining gap (HRS-002 conflict note names lib/couranr/conversations/states.ts).
- Constraint: no public phone (page comment agrees); messages never mutate state/price/address (registry :395). States: unread/response-pending/AI-answered(net-new, AIS-001 gates)/human-review/after-hours/closed. AI states blocked by AIS-001+FLG-001 (auto-reply default OFF at launch) — render only real conversation data.

MER-013 Website tools — `/business/website-tools` — PLACEHOLDER; NET-NEW (Phase 10)
- Merchant delivery link/QR/embed. Depends on hosted request route `/request/[merchantSlug]` (PUB-004, not implemented — screens.ts:52) and workspace slug (slug derivation exists: workspace.ts:97-105; DB re-derives). Constraint: not a product checkout; customer request still requires merchant validation before any payment ask (CAP-001: "customer_payment_before_merchant_validation": false; spec 01:277). States: draft/published/invalid-embed/disabled. Defer behind PUB-004; safe behavior now: copy-link for slug + "coming after activation" empty states.

MER-014 Merchant settings — `/business/settings` — PLACEHOLDER
- Real data: the workspace row (category, pickup_address, contact_phone, payer_default, policies version/date). Net-new: named update commands (none exist — workspace has create only; grep shows no update RPC). Constraint (registry :417): locked policy registry wins over mock values; NO subscription controls in pilot. States: saved/unsaved/verification-required/permission-denied. Roles: writes owner (manager arguable — undedecided beyond DRP-001 scope; safe default owner-only with a note), read any member.

MER-015 Team and permissions — `/business/settings/team` — PLACEHOLDER
- `business_members` table exists (read by actor.ts:125) with role owner|manager|dispatcher|viewer|billing and status active|invited|disabled (permissions.ts:11-12). Net-new: invite/resend/change-role/disable/remove commands + last-owner protection (registry :426). Display the TRM-002 + DRP-001 permission matrices verbatim — TRM-002 acceptance criterion: "Each of the five roles has an explicit permission set before MER-015 ships"; anything beyond conversations+requests is UNDEFINED and the screen must not promise it. Roles: owner manages team (net decision; safe default owner-only). States: pending-invitation/active/disabled/last-owner-protection.
- Verify: assert a demoted-to-viewer member loses conversation read (TRM-002 criterion) and request-create (DRP-001) in the same run.

MER-016 Billing settings — `/business/settings/billing` — PLACEHOLDER
- Real data available: payment obligations/attempts per request via the payments slice (lib/couranr/payments/commands.ts: getObligationForRequest, ensurePaymentObligation/Intent; migrations 20260731230000…20260801130000). Net-new: saved payment method (Stripe SetupIntent — note frozen stripe@15.12.0, pinned apiVersion 2024-04-10 per couranr stripe client lib/couranr/payments/stripe.ts), receipts list, refunds view (delivery refunds are Operations-only, REF-001). Constraints (registry :439): no monthly subscription invoice in pilot; delivery charge separate from product sale (REF-002); no tax line (TAX-001 unresolved). States: no-method/default-method/payment-failed/refund-pending/complete. Roles: billing + owner is the natural set but ONLY conversations (TRM-002) and requests (DRP-001) are decided — billing-role access to this screen is an OPEN decision; safe default owner+billing read, owner writes, flagged as assumption.

=== VERIFICATION OBLIGATIONS (every deliverable) ===
1. `git diff` re-read per CLAUDE.md protocol; enumerate enforcement-point siblings (browser vs service-role client; route guard vs SQL guard — the codebase pattern is BOTH, e.g. onboarding/commands.ts:89-110).
2. New tables: additive migration, reviewed before apply, `revoke all … from public, anon, authenticated, service_role; grant … to service_role` explicitly (pg_default_acl on the connected project grants arwdDxtm to all three roles — a narrow GRANT alone is a no-op), RLS enabled, prove with `has_table_privilege`.
3. Browser runs on the disposable stack (`e2e/disposable/up.mjs` + `gateway.mjs`; auth refusals proved by `authGateway.mjs` before citing any authenticated result), row assertions via psql, screenshots mapped to assertions, synthetic-marker seeds, cleanup reported. The gateway bypasses the production network path — state that gap in reports.
4. Composite RPC results: test existence with `row is distinct from null` (four couranr_* command functions return composites).