# BUILD CONTRACT — MER-013 Website tools (`/business/website-tools`)

All claims below were verified by commands run in this session; citations are file:line or a SQL query whose output I saw.

## 1. Authority stack (verified present)

- `02_DECISION_REGISTRY.json` **exists at repo root** (43 decisions; CLAUDE.md's "does not exist" is stale). **No decision references MER-013** — `python3` scan of `affected_screen_ids` returned `[]` for MER-013. Relevant decisions: PAY-001 (payer default, decided), TRM-001 (institutional language, no unverified proof claims), TRM-002 (decided — but its permission matrix covers **conversations only**), DRP-001 (decided — request create/submit roles only), FLG-002 (**unresolved** — feature-flag storage), MKT-001, SVC-002 (unresolved market boundaries).
- `UI_SCREEN_REGISTRY.md:398-407` — MER-013: route `/business/website-tools`, Core / Phase 10, purpose "merchant delivery link, QR code, embed/button tools, branding, and customer request defaults"; allowed actions "Copy link; download QR; configure form/button; preview; publish"; **required states: Draft; published; invalid embed settings; disabled**; authority Spec §§7, 16; constraint: **"Do not turn Couranr into the merchant's product checkout. Customer request still requires merchant validation."** Registry:80: desktop-first responsive.
- `Couranr_Claude_Code_Master_Package.md:613` names the route; `:938` puts "website link/QR, attribution, conversion" in **Phase 10**; `:76` "Couranr does not need to become the merchant's ecommerce platform."
- `couranr_claude_code_package/01_MASTER_IMPLEMENTATION_SPEC.md:266-279` — hosted-request flow: customer opens **merchant delivery link** → submits order reference + details → **merchant validates** → quote → customer authorizes → "Never request customer payment before merchant validation." `:553` public route `/request/[merchantSlug]`.
- Adjacent constraints: registry `UI_SCREEN_REGISTRY.md:285` ("advanced website tools … belong after dashboard entry"), `:296` ("Do not require website tools … to activate"), `:922` (branded tracking is post-MVP; "basic branding may live inside Website Tools"), `:934` (`website_tools_dashboard_ui.png` is reference-only; MER-013 canonical).

## 2. Mocks

`docs/couranr-mvp/ui-reference/CANONICAL_SCREEN_SOURCE_MAP.tsv` rows 8, 24, 30, 65 map MER-013 to four root UUID PNGs — `05AC4274-…​.png` (desktop), `288BA2E3-….png` (variant B), `4E254F81-….png` ("Delivery Link & QR Builder", variant C), `A2961811-….png` ("Branded Tracking Page", variant D) — **all four exist on disk at repo root** (verified `ls`). The registry's canonical path `canonical-mvp-images/merchant/MER-013_website-tools.png` **does not exist on disk** (only `PUB-004_…png` exists under `canonical-mvp-images/public/`). Variant D is post-MVP per registry:922 — use only its basic branding elements.

## 3. What exists today (verified)

- **Page**: `app/(couranr)/business/website-tools/page.tsx` is a 17-line `ScreenPlaceholder` (screenId MER-013). `lib/couranr/screens.ts:72` marks it `implemented: false`. PUB-004 `/estimate` is **also a placeholder** (`app/(couranr)/(public)/estimate/page.tsx`, 17 lines) and **no `/request/[merchantSlug]` route exists** (no `(public)/request` dir).
- **Slug — YES, it exists and is populated.** `business_accounts.slug` is `text NULL` with `UNIQUE` constraint `business_accounts_slug_key` (live catalog query on project `zrdxlrlqxdslqpnoqmus`). Live data: **13 accounts, 0 null slugs**. Slugs are minted transactionally by `couranr_create_merchant_workspace` (`supabase/migrations/20260731061356_couranr_merchant_workspace.sql:174-201` — normalize, retry `-2…-5`, then random 8-char suffix), fed from `lib/couranr/onboarding/workspace.ts:58,226` (`toSlugBase`) via `lib/couranr/onboarding/commands.ts:116`.
- **But no API exposes the slug.** `grep -rn slug app/api` → zero hits. `listActiveMemberships` (`lib/couranr/requests/actor.ts:174-193`, serving `GET /api/couranr/me/business-accounts`) selects only `business_account_id, role, business_accounts(name)`. **Net-new: add `slug` to that select** (one-line, server-side, tenant-scoped already).
- **Defaults**: `couranr_merchant_workspaces` holds `payer_default` (`merchant|customer`, migration :107) and `pickup_address`, but the only TS reader is `lib/couranr/dispatch/commands.ts`; `POST /api/couranr/me/workspace` exists, **no GET**. Net-new: workspace read endpoint.
- **Branding/publish state**: **no storage exists.** Migration comment :114: "no Stripe setup, no logo". `grep logo|brand|publish` across migrations → nothing. Draft/published/disabled state needs a **net-new table** (e.g. `couranr_website_tool_configs`, one row per business_account, additive migration following the house pattern: RLS-enabled/no-policy, revoke PUBLIC/anon/authenticated, narrow service_role grant — the `pg_default_acl` caveat documented in migration :26 applies).
- **Real working merchant machinery MER-013 can link into** (all real routes, verified file list): delivery-request draft/submit (`app/api/couranr/delivery-requests/route.ts`, `[id]/submit`), estimate, payment-link, authorize-payment, readiness, fulfillment; conversations (`/api/couranr/conversations/*`); landing/tenant resolution (`/api/couranr/me/*`). Server-side pricing constants governed by the registry live in `lib/couranr/public/governed.ts:18-45` (PRC-001 $22.99 / MIL-001 3 mi / MIL-002 tiers) with `tests/couranr-public-claims.test.ts` asserting registry agreement.
- **Analytics**: `20260806120353` creates only empty `private` and `analytics` schemas — **no link/QR attribution tables exist. Never render scan counts, click counts, or conversion numbers.**

## 4. Per-section contract

File: rewrite `app/(couranr)/business/website-tools/page.tsx` (client page inside the existing `(couranr)/business` shell, Bearer-token fetch pattern used by sibling real pages `business/deliveries/new`, `business/messages`). New API: `app/api/couranr/merchant/website-tools/route.ts` (GET/PUT), reusing `resolveRequestActor`/membership plumbing from `lib/couranr/requests/actor.ts`.

| Section | Content | Data source (real only) | Net-new |
|---|---|---|---|
| Hosted link | `https://<host>/request/<slug>`, copy button, **"Not yet live" badge until PUB-004 ships** | slug via extended `listActiveMemberships` select | slug in API response |
| QR | Client-side QR of the same URL, PNG/SVG download. No QR dep exists in `package.json` — either vendor a tiny generator or render server-side; adding a dependency is a decision to surface | derived from slug | QR generation |
| Embed/button | Copyable `<a>`/`<iframe>` snippet generator with live preview; "invalid embed settings" = client-side validation state (bad size/color values) | pure client derivation from slug + config | all |
| Branding | Basic only (name shown from `business_accounts.name`; color/logo **deferred** — no storage, registry:922 puts advanced branding post-MVP) | name from memberships API | storage if built |
| Request defaults | Read-only display of onboarding `payer_default` + pickup address (PAY-001: onboarding captures a default only); editing belongs to MER-014 settings | net-new GET on `couranr_merchant_workspaces` | GET endpoint |
| Publish state | Draft / Published / Disabled toggle | net-new `couranr_website_tool_configs` | table + migration + GET/PUT |

**Required-state reachability:** Draft = config row absent or `status='draft'` (default for every existing merchant — this is the honest empty state, with "Publish" as next action). Published = row `status='published'`. Disabled = row `status='disabled'` **and must be forced/labelled while PUB-004 is unshipped — never hand a merchant a link that 404s as "published"**. Invalid embed = client validation, reachable by typing bad values. Loading/error states: fail closed like `me/business-accounts` (route comment :19-23 — a failed lookup is a 500, never an empty list).

**Roles/tenant:** TRM-002's decided matrix is conversation-scoped (`02_DECISION_REGISTRY.json` TRM-002 value; `lib/couranr/conversations/commands.ts:548,586`); DRP-001 covers request create/submit (`lib/couranr/requests/permissions.ts:1-9,41`). **No authority assigns website-tools permissions — this is a gap, not a decision.** Safe behavior pending a registry entry: all active members read; owner+manager write/publish (settings-like, so exclude dispatcher); viewer/billing read-only; enforce **server-side in the PUT route** via the membership pattern, never in the client. Tenant scoping: server derives account from membership; client never states its tenant (`me/business-accounts` route comment :15-18).

**Gating open decisions:** (a) website-tools role matrix — unresolved, propose registry entry; (b) FLG-002 unresolved — do not build a feature-flag system for publish; a status column on the config row suffices; (c) QR dependency addition; (d) PUB-004 itself — the entire "published live link" outcome. TRM-001 applies to all copy (no proof claims, no counts).

## 5. Honestly buildable before PUB-004

Buildable now: slug surfacing, copy-link, QR, embed generator, preview, draft/publish storage, defaults display, role enforcement — everything except a link that actually resolves. The link/QR/embed must carry an explicit "goes live when hosted requests launch" state; publishing may be allowed but must render as "published (pending launch)" or publish stays disabled — pick one and say so in copy. Do **not** stub `/request/[merchantSlug]` with a fake form: spec :266-279 requires the full merchant-validation flow, which is PUB-004's contract, not MER-013's.

## 6. Verification obligations

1. Disposable stack (`e2e/disposable/up.mjs`, `gateway.mjs`; `authGateway.mjs` proves the auth shim refuses before any authenticated assertion is trusted — its header comment :1-24 is the contract). Seed two businesses + members in every role via `bootstrap.sql` pattern; row-assert with `psql` from `dbTest.mjs`.
2. Browser (global Playwright, `/opt/pw-browsers`): sign in as owner → page shows the real slug URL matching a `select slug from business_accounts` row assertion; copy-link clipboard content equals it; QR decodes to it (decode in Node, don't eyeball); publish → row assertion `status='published'`; viewer/billing → PUT returns 403 (browser sees denial, DB row unchanged); member of business A cannot read/write B's config (cross-tenant row assertion); invalid embed values → inline error, no request fired.
3. Screenshot each required state (draft, published/pending-launch, invalid-embed, disabled, empty/no-workspace) with its paired assertion.
4. Unit tests: permission matrix for the new capability (mirroring `tests/` DRP-001 style) and slug-URL derivation.
5. Report gaps: the disposable auth gateway bypasses production GoTrue (named limitation), and PUB-004 end-to-end (link → hosted form) is unverifiable until that screen exists.

Key paths: `/home/user/couranr-os/app/(couranr)/business/website-tools/page.tsx`, `/home/user/couranr-os/lib/couranr/requests/actor.ts:174-193`, `/home/user/couranr-os/lib/couranr/requests/permissions.ts`, `/home/user/couranr-os/supabase/migrations/20260731061356_couranr_merchant_workspace.sql`, `/home/user/couranr-os/lib/couranr/onboarding/workspace.ts`, `/home/user/couranr-os/lib/couranr/public/governed.ts`, `/home/user/couranr-os/e2e/disposable/`, `/home/user/couranr-os/02_DECISION_REGISTRY.json`, `/home/user/couranr-os/UI_SCREEN_REGISTRY.md:398-407`.

Unverified: whether the four MER-013 root PNGs' pixel content matches the section list above (I confirmed disk existence and TSV mapping, not image content); whether `supabase/migrations` history exactly matches the live project's applied migrations (I verified live schema facts directly by catalog query instead).