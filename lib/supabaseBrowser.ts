"use client";

import { // lib/supabaseBrowser.ts
"use client";

import { createClientComponentClient } from "@supabase/auth-helpers-nextjs";

export const supabaseBrowser = createClientComponentClient(); } from "@supabase/auth-helpers-nextjs";

export const supabaseBrowser = createClientComponentClient();